import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Project({}: Props): ReactElement {
    return (
        <div>
            project c0tent
        </div>
    )
}
