import React from "react";

type Props = {};

export default function Logo({}: Props) {
  return (
    <div
      style={{
        minWidth: "300px",
        userSelect: "none",
      }}
    >
      高效ToDo
    </div>
  );
}
