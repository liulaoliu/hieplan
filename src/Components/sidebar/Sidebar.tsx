import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Sidebar({}: Props): ReactElement {
    return (
        <div>
            我是sidebar
        </div>
    )
}
